"""Use-case for extracting bank transactions from emails."""


def extract_transactions(bank: str, email: str, days_from: int, output: str) -> None:
    """Temporary placeholder while legacy flow is migrated.

    Phase 2 keeps compatibility with the current execution path and prepares
    the target application orchestration boundary.
    """
    _ = (bank, email, days_from, output)
